import 'package:flutter/material.dart';

class Emergency_Contacts extends StatelessWidget {
  const Emergency_Contacts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
